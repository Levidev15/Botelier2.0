- [Integration response-mapping precedence & OHIP/GC shapes](integration-response-mapping.md) — one shared `extract_json_value` (strips `$.`); node mapping overrides seed, so seed + store.ts template + saved `flow_versions` rows must change in lockstep; OHIP/GuestCentric response wrapper quirks (raw arrays, per-endpoint body envelopes).
- [Simulator API node stall](simulator-api-node-stall.md) — API_REQUEST nodes require forced tool_choice in simulation.py; "auto" lets LLM return text and stall.
- [Query-param overrides & api-tester secrets gap](query-param-overrides.md) — all 3 API-request paths thread query_param_overrides through one _build_url; api-tester deliberately skips {{secrets.*}} substitution so secret overrides fail in test but work live.
- [Simulator↔live parity](simulator-live-parity.md) — sim runs the resolved assistant's llm_model (never a hardcoded stronger model); assistant lookup by tool_set_id must also filter account_id (ownership unvalidated).
- [Credential encryption architecture](credential-encryption.md) — all models use botelier/crypto.py; MultiFernet with comma-separated key list supports zero-downtime rotation.
- [Call Duration surfaces](call-duration-surfaces.md) — display Duration = AI leg (pipecat), not CallLog.duration_seconds (billing-only); all surfaces (to_dict/analytics/CSV/frontend) must use identical duration_source filters in lockstep.
- [Per-property data isolation](per-property-isolation.md) — (account,property) scope enforced fail-closed at the IntegrationClient chokepoint (not FlowExecutor init); does NOT cover legacy custom-HTTP tools or MCP.
- [Integration adapter layer](integration-adapter-layer.md) — per-vendor auth/refresh behind IntegrationClient (integration_runtime/adapters); new auth_types need an adapter+registry entry or silently run token-less.
- [Canonical PMS domain schemas](canonical-pms-schemas.md) — vendor-agnostic entities normalized inside adapters, opt-in via seed `canonical_entity`; None=wrapper absent vs []=zero records; cross-vendor parity is the contract; certified adapters only.
- [flow_versions version_number invariant](flow-version-numbering.md) — never reassign a draft's version_number on save; derive new-draft numbers from max(version)+1; save/publish/revert must stay consistent or uq_tool_version 500s.
- [flow-editor legacy node shapes](flow-editor-legacy-node-shapes.md) — old flow_versions rows have partial node data; node components must default each nested array/object (not just the top-level obj) or loading an old version crashes the editor.
- [confirmation branch handle resolution](flow-confirmation-branch-handles.md) — seeded confirmation→next edges often lack sourceHandle="confirmed"; strict get_next_node then stalls the flow so the booking POST never fires. Engine falls back to any non-"edit" edge.
- [Integration connection_config](integration-connection-config.md) — non-secret per-connection constants use required_fields `"storage":"connection_config"`; `_build_url` bridges camelCase→snake & MUST replace `{{..}}` before `{..}`; path + query-param precedence must match (conn_config first).
- [Flow function gating vs live/simulator sync](flow-function-gating.md) — get_function_schemas() feeds live voice AND simulator; gating its exposure requires paired refresh in function_mapper (any node advance, not just collected) + per-iteration rebuild in simulation, else hang / OpenAI 400.
- [Call lifecycle webhooks & pipeline teardown](call-lifecycle-webhooks.md) — caller hangup reliably fires only /status (not connect-complete); it's the backstop that must cancel the pipeline + enqueue ACW; never tear down transfers; is_pipeline_active is per-worker.
- [Deep-link mount effects vs account-context](frontend-deeplink-account-context.md) — one-shot ?param deep links must gate on accountId (hydrated async in mount effect) before burning their ref guard; mirror call-logs pattern.
- [SAVE_RECORD & live-call sessions](save-record-live-session.md) — live-call FlowExecutor has db_session=None; DB-writing flow handlers must open own SessionLocal; cold transfers skip /connect-complete; flow_executor logger caveats.
- [JWT auth query params](jwt-auth-query-params.md) — creds like apikey must ride JWT login/refresh too; declared via auth_config['auth_request_query_params']; 3 JWT paths change in lockstep.
- [Integration connection_config variable injection](integration-connection-config-vars.md) — property-level constants (hotel_id, hotel_name, currency) belong in connection_config; 3 code layers now read it in priority order.
- [OAuth callback completion & refresh locking](oauth-callback-completion.md) — code exchange happens on an authenticated frontend completion endpoint (check order sacred); callback is a stateless hop to config-only FRONTEND_URL; refresh never runs unlocked.
- [Integration resilience & 3-legged OAuth2](integration-resilience.md) — Postgres cross-worker rate-limit/breaker gates inside IntegrationClient (certified-only, fail-open, own SessionLocal); one execute_request=one breaker verdict; idempotent-only retries; oauth2 authorization_code transient-vs-terminal + nonce-bound public callback.
- [Voice barge-in gating](voice-barge-in-gating.md) — Pipecat fires InterruptionFrame on EVERY user turn; interrupted-marking must gate on bot-speaking; toggle = AlwaysUserMuteStrategy, never enable_interruptions=False.
- [Designable PMS payment page](designable-payment-page.md) — PMS-native single-call booking+charge vs Stripe fallback (ambiguity→link); card never stored/logged; link burns only on TERMINAL failure (in-progress status 0 must NOT); public page is hostile-input: submit-key allowlist + branding CSS/URL sink coercion.
- [Live flow guidance & record sync](flow-live-llm-guidance.md) — flow trigger speaks via TTS then MUST run_llm=False (else double greeting); node instructions ride function results (node_instructions/current_node_context); saved records re-synced post-function via reserved _saved_records snapshot key.
- [Universal API Adapter (DYNAMIC_OPERATION)](universal-api-adapter.md) — IntegrationAPIConfig has no integration_type_id/property_id fields; check_account_permission is in auth.middleware not auth.permissions; tool slug must be namespaced by connection to prevent cross-connection collisions.
- [Basic auth query params (DefaultAdapter)](basic-auth-query-params.md) — `basic_auth_query_params` list in auth_config appends credential values as URL query params on every request; configured in Auth Settings UI, persisted by integration_builder, collected from user at connect time via required_fields.
- [Flow terminal-node & fallback-confirmation behavior](flow-terminal-node-behavior.md) — choice enums live at slot.validation.choices; confirm_details must be gated post-collection; END nodes need explicit "call end_call_<id>" instruction; tools.account_id resolves via ToolSet.
- [TTS token-mode text processing](tts-token-mode-substitutions.md) — TOKEN aggregation feeds run_tts sub-word fragments; whole-word regex substitutions silently no-op; buffer to whitespace + flush at end.
- [ACW topic & terminal skip states](acw-topic-terminal-states.md) — OpenAI strict json_schema rejects maxLength (cap output server-side); QA failures stamp skip_reason+completed_at, manual re-run gate = completed AND no skip_reason; CSV appends after Transcript only.
- [Lazy-import scope & silent tool drops](lazy-import-scope-voice-tools.md) — a method-local import never reaches sibling methods; voice per-tool schema errors are caught broadly, so a NameError silently drops all non-flow tools ("Failed to build schema" + 0 tool calls).
- [Speech-gated call teardown](voice-speech-gated-teardown.md) — never REST-hangup/transfer inline after TTSSpeakFrame; use post-speech callback + playback mark; watchdogs must be speech-aware with a hard cap.
- [Spec import fail-closed & format detection](spec-import-fail-closed.md) — detected content wins over declared format chip; zero-endpoint parses must raise before DB writes; integration_types.spec_version is varchar(64) (Postman schema URL overflows).
- [pipecat fork resolution & Flux turn strategies](pipecat-fork-and-flux-turn-strategies.md) — dev imports pip pipecat, prod Docker imports src/ fork via PYTHONPATH; Flux paths must pass explicit ExternalUserTurnStrategies, never None (None → SmartTurn default → transformers crash).
- [FastAPI router ordering — static vs wildcard conflicts](fastapi-router-ordering.md) — static paths in a later router lose to wildcards in an earlier router; integration_builder_router must be registered before integrations_router in main.py.
- [MCP channel lifecycle & Pipecat schemas](mcp-channel-lifecycle.md) — Pipecat MCP schemas must enter the initial LLMContext; AnyIO MCP sessions must open/use/close in one task; native names win.
- [Voice dropout invariants](voice-dropout-hardening.md) — false interruptions, loop starvation, and TTS micro-fragmentation are all heard as speech cutting out; gates/clamps/replica-pin rules that must not regress.
- [Next.js dev cache self-corruption](nextjs-dev-cache-corruption.md) — pages 200 but all /_next/static 404 + webpack-runtime MODULE_NOT_FOUND = Next 14.2.0 dev bug; upgrade patch version, cache clear alone recurs.
- [Adapter runtime guardrails](adapter-runtime-guardrails.md) — auth headers always win over config headers; path substitutions URL-encoded; response cap is transport-level pre-parse; test_operation drafts are preview-only.
- [Voice API completion bridges](voice-api-completion-bridges.md) — a wait greeting requires caller-safe speech when API returns; preserve raw mapping for LLM narration, and sort only timestamp-backed transcript text.
- [Live flow execution hardening](live-flow-execution-hardening.md) — parallel_tool_calls=False; direct-speech guarantee must route silent END/TRANSFER landings through the real terminal handler, not just speak text; graph_exhausted ≠ is_complete; flow_sessions abandon via complete_call.
- [Replit default webview/preview port selection](replit-default-webview-port.md) — `[[ports]]` file order + `exposeLocalhost` flags can override a workflow's `outputType=webview`; reorder/dedupe ports, never strip `exposeLocalhost` from a localhost-only bind.
- [Call-scoped flow facts and replay safety](call-scoped-flow-facts.md) — share only explicit caller facts across flows; keep defaults/derived values local, and make side effects atomically idempotent across workers.
- [Confirmation handler merge](confirmation-handler-merge.md) — `_run_confirmation_logic` is the canonical handler; both entry points delegate; edge-fallback guard must use `_confirmed_branch_next_node()` not strict `get_next_node`.
- [Deepgram Flux TTS integration](deepgram-flux-tts.md) — /v2/speak, Interrupt not Clear, SpeechMetadata = turn end; pip pipecat 1.5.0 needs manual file copy for dev.
- [MESSAGE node advance gating](message-node-advance-gating.md) — MESSAGE nodes expose no LLM function; a dead-end message chain lets the LLM fabricate outcomes before hanging up unless gated like end_call.
- [API voice_result narration boundary](api-voice-result-narration-boundary.md) — one shared `voice_result` field held both genuine designer narration and a raw internal data-digest fallback; split by an explicit `voice_result_is_auto_summary` flag, not by non-emptiness.
- [Direct-speech transcript anchoring](flow-direct-speech-transcript-anchoring.md) — manually-pushed TTSSpeakFrame text (prompts/bridges) needs a real-timestamp capture into pending_responses, or transcript ordering can invert question/answer.
- [TTS config knob propagation](tts-config-knob-propagation.md) — live-engine TTS knobs (speed, expressivity, etc.) also need adding to greeting_cache.py's separate REST path + cache key; UI gating must check the specific voice/model, not a shared provider boolean.