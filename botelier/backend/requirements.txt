fastapi>=0.115.6
uvicorn[standard]>=0.24.0
sqlalchemy==2.0.23
psycopg2-binary==2.9.9
pydantic[email]>=2.10.6
python-multipart>=0.0.22
twilio==9.0.0
pipecat-ai[deepgram,websocket]==1.5.0
# SmartTurn v3 (LocalSmartTurnAnalyzerV3) needs transformers' WhisperFeatureExtractor.
# ONNX-based — torch is NOT required (onnxruntime + soxr already ship with pipecat-ai).
# Pinned to the dev-verified version so the Docker image reproduces exactly what was tested.
transformers==5.3.0
anthropic>=0.74.0
aiohttp>=3.13.2
loguru>=0.7.3
mcp>=1.26.0
httpx>=0.27.0
beautifulsoup4>=4.12.0
python-jose[cryptography]>=3.3.0
bcrypt>=4.0.0
sse-starlette>=2.1.0
openai>=1.0.0
requests>=2.31.0
azure-identity>=1.19.0
azure-keyvault-secrets>=4.9.0
num2words>=0.5.13
